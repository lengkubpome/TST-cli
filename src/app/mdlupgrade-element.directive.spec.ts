/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { MDLUpgradeElement } from './mdlupgrade-element.directive';

describe('Directive: MDLUpgradeElement', () => {
  it('should create an instance', () => {
    let directive = new MDLUpgradeElementDirective();
    expect(directive).toBeTruthy();
  });
});
